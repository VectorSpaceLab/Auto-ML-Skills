# Repository Provenance

This skill was generated from MMDetection repository evidence and live package inspection.

## Source Snapshot

| Field | Value |
| --- | --- |
| Project | MMDetection |
| Canonical skill id | `mmdetection` |
| Package distribution | `mmdet` |
| Package version | `3.3.0` |
| Git branch | `main` |
| Git commit | `cfd5d3a985b0249de009b67d04f37263e11cdf3d` |
| Exact tag | none detected |
| Working tree state | clean at initial scope capture; generated `skills/` artifacts were added during skill creation |
| Remote URL | omitted-private-or-unknown |

## Relative Evidence Paths

Primary evidence used:

- `README.md`
- `setup.py`, `setup.cfg`, `MANIFEST.in`, `requirements/*.txt`
- `mmdet/`
- `configs/`, `mmdet/configs/`, `model-index.yml`, `dataset-index.yml`
- `docs/en/`
- `demo/`
- `tools/`
- `tests/`
- `projects/example_project/` and selected `projects/*/README.md`

Excluded or de-prioritized evidence:

- `.git/`, build/cache directories, generated bytecode, and local environment files.
- `.github/`, `.circleci/`, `.dev_scripts/`, and `docker/` except as maintainer context.
- `docs/zh_cn/` except as duplicate-language cross-checks when English docs are missing a fact.
- Large media/checkpoint/dataset artifacts; this skill does not bundle binary assets.

## Live Inspection Baseline

The private inspection environment verified:

- `mmdet` import and metadata version `3.3.0`.
- `mmcv 2.1.0`, `mmengine 0.10.7`, `torch 2.1.0+cpu`, and `numpy 1.26.4` compatibility for import/API inspection.
- Public APIs: `DetInferencer`, `init_detector`, `inference_detector`, `DetDataSample`, and `HorizontalBoxes`.
- `pip check` passed after installing full `mmcv`, pinning compatible NumPy/OpenCV wheels, and avoiding `mmcv-lite` for `mmcv.ops` workflows.

Do not copy private environment paths or local install commands from inspection artifacts into runtime guidance. Use this provenance only to decide whether the skill should be refreshed when MMDetection changes.

## Staleness Signals

Refresh this skill when any of the following changes materially:

- MMDetection major/minor version or public API signatures.
- Required `mmcv`, `mmengine`, `torch`, NumPy, or OpenCV compatibility bands.
- Config layout, model-index/metafile schema, launcher flags, or inference APIs.
- Dataset/evaluator config keys or registry/default-scope behavior.
- Source scripts in `demo/`, `tools/`, or dataset converters that this skill adapted or distilled.
