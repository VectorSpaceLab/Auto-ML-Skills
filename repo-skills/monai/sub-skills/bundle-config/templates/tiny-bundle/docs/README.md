# Tiny MONAI Bundle

This synthetic bundle is for parser, metadata, and CLI experiments only. Replace placeholder metadata versions and license text before using it as a real bundle.

Example parser check:

```bash
python -m monai.bundle run --config_file configs/inference.json --meta_file configs/metadata.json --workflow_type infer
```
