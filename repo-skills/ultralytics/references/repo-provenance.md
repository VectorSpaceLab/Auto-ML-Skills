# Repo Provenance

- Schema: `skillsmith.repo-provenance.v1`

## Source Snapshot

- Repository: Ultralytics YOLO Python package.
- Generated skill id: `ultralytics`.
- Source commit: `7413c429a6a8406ef3003364bf98f7cfef0c260d`.
- Source branch: `main`.
- Exact tag: none detected at generation commit.
- Working tree state at generation: dirty because `skills/` contained untracked generated/review artifacts.
- Remote URL: omitted-private-or-unknown.
- Package version used for live inspection: `8.4.72`.

## Evidence Paths

- `pyproject.toml` for package metadata, dependencies, optional extras, console entry points, build backend, and test/style configuration.
- `README.md` for public install, CLI/Python quickstart, model-family overview, and download behavior.
- `ultralytics/` for public APIs, model classes, engine workflows, config parsing, results objects, trackers, solutions, data utilities, exporter, and testable runtime behavior.
- `ultralytics/cfg/default.yaml`, `ultralytics/cfg/datasets/*.yaml`, `ultralytics/cfg/models/**/*.yaml`, and `ultralytics/cfg/trackers/*.yaml` for config defaults and templates.
- `docs/en/` for public task, mode, model, integration, deployment, training, tracking, solution, and contributor guidance.
- `examples/` for native example candidates and workflow intent; large media/UI/C++/Rust examples were treated as reference-only evidence.
- `tests/` for behavior, edge cases, focused native verification candidates, and repo-development test selection.
- `skills/ultralytics/` for existing repo-local agent guidance and compatible routing patterns.

## Extraction Scope

Included: package source, configs, docs, examples, tests, package metadata, and existing repo-local skill evidence.

Excluded: VCS internals, build/dist outputs, caches, generated bytecode, broad CI/release infrastructure except as repo-development evidence, review/test artifacts, and native examples that require network, credentials, display/UI, long training, large downloads, or external services.

## Refresh Triggers

Refresh this skill when `ultralytics/__init__.py` version changes, CLI tasks/modes change, `pyproject.toml` optional extras change, model-family docs add/remove public classes, `Results` fields change, tracker or solution YAMLs/classes change, exporter formats/dependencies change, or tests/docs reveal new public workflow behavior.
